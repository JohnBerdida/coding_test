{
    "name": "cs-coding-challenge",
    "version": "1.0.0",
    "scripts": {
        "build": "tsc",
        "start": "node dist/server.js",
        "dev": "nodemon --watch src --ext ts --exec 'npm run clean && npm run build && node dist/server.js'",
        "clean": "rimraf dist"
    },
    "dependencies": {
        "dotenv": "^16.4.7",
        "express": "^4.18.2",
        "reflect-metadata": "^0.2.2"
    },
    "devDependencies": {
        "@types/express": "^4.17.17",
        "@types/node": "^20.8.2",
        "nodemon": "^3.0.1",
        "rimraf": "^5.0.1",
        "typescript": "^5.7.3"
    }
}
